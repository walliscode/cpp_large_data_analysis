#include <filesystem>

namespace config {
////////////////////////////////////////////////////////////
/// \brief return folder path for data,
/// this should be used for other paths
///
////////////////////////////////////////////////////////////
const std::filesystem::path getDataFolder();

/////////////////////////////////////////////////
/// @brief Returns the ui folder under the data folder
/////////////////////////////////////////////////
const std::filesystem::path getUIDataFolder();
} // namespace config
